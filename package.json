{
  "name": "@saulx/fs-walker",
  "version": "1.0.1",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "build": "npx tsc",
    "watch": "npm run build -- --watch",
    "clean": "rimraf {.turbo,dist,node_modules}",
    "test": "ava --verbose"
  },
  "author": "",
  "license": "UNLICENSED",
  "ava": {
    "files": [
      "test/**/*.ts"
    ],
    "match": [
      "!_*"
    ],
    "concurrency": 1,
    "extensions": [
      "ts"
    ],
    "require": [
      "ts-node/register"
    ]
  },
  "dependencies": {
    "@saulx/utils": "2.4.1"
  },
  "devDependencies": {
    "@types/node": "^17.0.25",
    "ava": "^4.3.1",
    "rimraf": "^3.0.2",
    "ts-node": "^10.1.0",
    "typescript": "^4.3.5"
  }
}
